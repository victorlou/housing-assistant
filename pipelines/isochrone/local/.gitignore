# Local Python env
.venv/

# Input data — large or transient, not committed
data/*.osm.pbf
data/*.zip

# Output of the compute step (gets uploaded to a Databricks volume instead)
*.parquet

# r5py's compiled routing graph cache (sits next to the OSM file)
data/*.mapdb
data/*.mapdb.p
data/network.dat
