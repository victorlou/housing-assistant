"""
Shared ingestion helpers.

Empty for now. We extract common patterns here once we have 2-3 sources and the
right abstraction shape is obvious in retrospect (HTTP fetch with retry,
timestamped volume writes, zip extraction, ingest_runs logging, etc.).
"""
