# Data

This folder is **not** for raw data. Raw data lands in Unity Catalog managed volumes.

The canonical open-data source catalogue is [`docs/data-sources.md`](../docs/data-sources.md). Each pipeline documents its source in `pipelines/<name>/README.md`.
